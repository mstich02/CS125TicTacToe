#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
#include <time.h>

int easyMode(int matrix, int size, int dimention){
	int randX = rand()%(size);
	int randY = rand()%(size);
	int randZ = rand()%(dimention);
	
	while (errorCheck(randY,randX,randZ) == 0){
      		int randX = rand()%(size);
       		int randY = rand()%(size);
	        int randZ = rand()%(dimention);
	}
//	return position
}

int hardMode(matrix, size, dimentions, turn
// if player places on corner
int matrix
int size
	if(matrix[0][0] = change 
	 
// i fplayer places on edge
// if player places on middle
 
int main(){
	srand(time(NULL));
}
